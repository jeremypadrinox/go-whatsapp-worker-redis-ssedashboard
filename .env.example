# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=gowhatsapp_worker
DATABASE_USER=postgres
DATABASE_PASSWORD=password
DATABASE_SSL_MODE=disable

# WhatsApp Configuration - Single Endpoint (Legacy)
WHATSAPP_BASE_URL=https://wa.balaimedan.id
WHATSAPP_AUTH=Basic YWRtaW46R2hpb3ZhbjFA

# WhatsApp Configuration - Multiple Endpoints (New)
# If both WHATSAPP_ENDPOINTS and WHATSAPP_AUTHS are provided, they will be used instead of single endpoint
# WHATSAPP_ENDPOINTS=https://wa.balaimedan.id,https://wa2.balaimedan.id,https://wa3.balaimedan.id
# WHATSAPP_AUTHS=Basic YWRtaW46R2hpb3ZhbjFA,Basic YWRtaW46R2hpb3ZhbjFB,Basic YWRtaW46R2hpb3ZhbjFC

# Worker Configuration
WORKER_CONCURRENCY=5
WORKER_BATCH_SIZE=10
WORKER_POLL_INTERVAL=1s
WORKER_MAX_RETRIES=3

# Rate Limiting Configuration
RATE_LIMIT_MIN_MESSAGES=1
RATE_LIMIT_MAX_MESSAGES=5
RATE_LIMIT_DELAY_MIN=2s
RATE_LIMIT_DELAY_MAX=5s

# Typing Delay Configuration
TYPING_DELAY_MIN=1s
TYPING_DELAY_MAX=3s

# API Configuration
API_HOST=0.0.0.0
API_PORT=8080

# Dashboard Configuration
ENABLE_AUTH=false
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=admin123
DASHBOARD_REALM=Go-WhatsApp Worker

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_READ_TIMEOUT=3s
REDIS_WRITE_TIMEOUT=3s
REDIS_IDLE_TIMEOUT=5m
REDIS_MAX_CONN_AGE=30m

# Redis Queue Configuration
REDIS_QUEUE_NAME=pending_messages
REDIS_QUEUE_PROCESSING_TIMEOUT=30s
REDIS_QUEUE_RETRY_DELAY=5s
REDIS_QUEUE_MAX_RETRIES=3
REDIS_QUEUE_BLOCKING_POP_TIMEOUT=0s
REDIS_QUEUE_BATCH_SIZE=10

# Redis Consumer Configuration
REDIS_CONSUMER_GROUP=workers
REDIS_CONSUMER_NAME=worker-1
REDIS_STREAM_MAX_LEN=100000

# Rate Profile Configuration
RATE_PROFILE=manual
RATE_DELAY_MEAN_MS=10000
RATE_DELAY_JITTER_MS=5000
RATE_BURST_LIMIT=20
RATE_COOLDOWN_SECONDS=210

# Bulk Configuration
BULK_CHUNK_SIZE=500